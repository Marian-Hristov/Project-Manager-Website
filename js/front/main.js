'use strit';

window.addEventListener("DOMContentLoaded", setup);

function setup() {
    actionMenuEvent();
    windowEvents();
    popupEvents();
    // enablePopUpClose();
    // enableValudationEvents();
    // ! REMOVE openPopUp lines
    // openPopUp("action");
    // openPopUp("add");
}