'use strit';

window.addEventListener("DOMContentLoaded", setup);

function setup() {
    changePageEvent();
    windowEvents();
    popupEvents();
    createProjectEvent();
    showTable(1);
    actionMenuEvent();
    searchEvent();
}