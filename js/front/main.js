'use strit';

window.addEventListener("DOMContentLoaded", setup);

function setup() {
    changePageEvent();
    windowEvents();
    popupEvents();
    createProjecEvent();
    showTable(1);
    actionMenuEvent();
}