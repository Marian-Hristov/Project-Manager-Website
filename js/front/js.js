/**
 * 
 * @param {HTMLElement} a 
 * @param {string} b 
 */
function lol(a, b) {
    a.style.color = b;
}

const body = document.querySelector('tbody');

for (let i = 0; i < 3; i++) {
    // body.innerHTML += body.innerHTML;
}